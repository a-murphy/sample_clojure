#new-build-flow
# Language setting
language: clojure

lein:
  - lein2

build:
#  pre_ci_boot:
#    image_name: drydock/u14cloall
#    image_tag: master
#    pull: true
  ci:
    - ls -l ~/lib
    - cd sample
    - lein test2junit
